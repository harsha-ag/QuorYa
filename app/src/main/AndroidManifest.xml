<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qnew">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Qnew">
        <activity android:name=".EnterAnswerActivity">

        </activity>
        <activity android:name="Comment.CommentActivity">

        </activity>
        <activity android:name=".AddQuestionActivity" />
        <activity android:name=".search.SearchActivity" />
        <activity
            android:name=".NavigateHomeActivity"
            android:label="@string/title_activity_navigate_home"
            android:theme="@style/Theme.Qnew.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ViewProfileActivity">

        </activity>
        <activity android:name=".singlequestion.ShowSingleQuestionActivity">

        </activity>


    </application>

</manifest>